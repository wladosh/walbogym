<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.walbogym.walbo.walbogym">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trainignsplanActivity" />
        <activity android:name=".selbsterstellen" />
        <activity android:name=".Statistik" />
        <activity android:name=".Motivation" />
        <activity android:name=".activityMotivation" />
        <activity android:name=".Trainingsplan_Einheiten" />
        <activity android:name=".Training_Trainingsplanauswahl" />
        <activity
            android:name=".activity_trainieren"
            android:label="@string/title_activity_trainieren"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity_trainingubung" />
        <activity android:name=".activity_trainingsstart"></activity>
    </application>

</manifest>